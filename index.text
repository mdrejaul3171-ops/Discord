<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>raizo | Premium Kids Wear</title>
    <link href="https://fonts.googleapis.com/css2?family=Jost:wght@300;400;500&family=Playfair+Display:ital,wght@0,400;0,600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>
    <style>
        :root {
            --bg-color: #ffffff;
            --text-main: #333333;
            --text-muted: #888888;
            --border-color: #f0f0f0;
            --card-bg: #f9f9f9;
            --accent: #d5c5b9; /* Soft taupe/beige accent */
        }
        
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Jost', sans-serif; background: var(--bg-color); color: var(--text-main); font-weight: 400; line-height: 1.6; }
        h1, h2, h3, .brand-logo { font-family: 'Playfair Display', serif; font-weight: 400; }
        
        /* Top Announcement Bar */
        .top-bar {
            background: var(--text-main);
            color: #fff;
            text-align: center;
            padding: 10px;
            font-size: 12px;
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        /* Minimalist Centered Header */
        header {
            padding: 30px 5%;
            display: flex;
            flex-direction: column;
            align-items: center;
            border-bottom: 1px solid var(--border-color);
            background: #fff;
            position: sticky;
            top: 0;
            z-index: 100;
        }
        .header-top {
            display: flex;
            justify-content: space-between;
            width: 100%;
            align-items: center;
            margin-bottom: 20px;
        }
        .brand-logo {
            font-size: 42px;
            color: var(--text-main);
            letter-spacing: 2px;
            cursor: pointer;
            text-transform: lowercase;
        }
        .header-icons {
            display: flex;
            gap: 20px;
            font-size: 18px;
            color: var(--text-main);
        }
        .header-icons i { cursor: pointer; transition: 0.3s; }
        .header-icons i:hover { color: var(--accent); }
        .cart-wrap { position: relative; cursor: pointer; }
        .cart-count {
            position: absolute;
            top: -8px; right: -12px;
            background: var(--text-main);
            color: #fff;
            font-size: 10px;
            padding: 2px 6px;
            border-radius: 10px;
        }

        /* Elegant Buttons */
        .btn-main {
            background: var(--text-main);
            color: #fff;
            border: none;
            padding: 15px 30px;
            font-family: 'Jost', sans-serif;
            font-size: 13px;
            letter-spacing: 1px;
            text-transform: uppercase;
            cursor: pointer;
            transition: 0.3s;
            border-radius: 2px;
        }
        .btn-main:hover { background: var(--accent); }
        .btn-outline {
            background: transparent;
            color: var(--text-main);
            border: 1px solid var(--text-main);
            padding: 15px 30px;
            font-family: 'Jost', sans-serif;
            font-size: 13px;
            letter-spacing: 1px;
            text-transform: uppercase;
            cursor: pointer;
            transition: 0.3s;
        }
        .btn-outline:hover { background: var(--text-main); color: #fff; }

        /* Boutique Product Grid */
        .section-title {
            text-align: center;
            margin: 60px 0 40px;
            font-size: 28px;
            color: var(--text-main);
        }
        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 40px 20px;
            padding: 0 5% 60px;
        }
        .product-card {
            cursor: pointer;
            text-align: center;
            transition: 0.3s opacity;
        }
        .product-card:hover { opacity: 0.8; }
        .product-img-wrap {
            background: var(--card-bg);
            padding: 20px;
            margin-bottom: 15px;
            position: relative;
        }
        .product-img-wrap img {
            width: 100%;
            height: 250px;
            object-fit: contain;
            mix-blend-mode: multiply;
        }
        .product-info h3 {
            font-family: 'Jost', sans-serif;
            font-size: 14px;
            font-weight: 400;
            color: var(--text-muted);
            margin-bottom: 5px;
        }
        .product-info p {
            font-size: 15px;
            color: var(--text-main);
        }

        /* Views & Modals */
        .view-section { display: none; animation: fadeIn 0.8s ease; }
        .view-section.active-view { display: block; }
        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }

        .modal { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(255,255,255,0.9); display: none; align-items: center; justify-content: center; z-index: 3000; }
        .modal-content { background: #fff; padding: 40px; width: 90%; max-width: 450px; box-shadow: 0 15px 40px rgba(0,0,0,0.08); position: relative; border: 1px solid var(--border-color); }
        .close-modal { position: absolute; top: 15px; right: 20px; font-size: 28px; cursor: pointer; color: var(--text-muted); }

        input {
            width: 100%; padding: 15px; margin-bottom: 15px;
            border: 1px solid var(--border-color);
            background: var(--card-bg);
            font-family: 'Jost', sans-serif;
            outline: none;
        }
        input:focus { border-color: var(--text-main); }

        /* Sleek Chat */
        .chat-fab-premium { position: fixed; bottom: 30px; right: 30px; background: var(--text-main); color: #fff; width: 55px; height: 55px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 20px; box-shadow: 0 10px 25px rgba(0,0,0,0.15); cursor: pointer; z-index: 1000; transition: 0.3s; }
        .chat-fab-premium:hover { transform: translateY(-3px); background: var(--accent); }
        .chat-container { max-width: 500px; margin: 40px auto; height: 60vh; border: 1px solid var(--border-color); display: flex; flex-direction: column; background: #fff; }
        .chat-header-premium { padding: 20px; border-bottom: 1px solid var(--border-color); text-align: center; }
        .chat-messages-area { flex: 1; padding: 20px; overflow-y: auto; background: var(--card-bg); display: flex; flex-direction: column; gap: 15px; }
        .chat-bubble { max-width: 75%; padding: 12px 18px; font-size: 14px; border-radius: 4px; }
        .chat-user { align-self: flex-end; background: var(--text-main); color: #fff; }
        .chat-admin { align-self: flex-start; background: #fff; border: 1px solid var(--border-color); }
        .chat-input-box { display: flex; padding: 15px; border-top: 1px solid var(--border-color); gap: 10px; background: #fff; }

        .payment-card { display: flex; justify-content: space-between; padding: 15px; border: 1px solid var(--border-color); margin-bottom: 10px; cursor: pointer; transition: 0.3s; }
        .payment-card.selected { border-color: var(--text-main); background: var(--card-bg); }
        .payment-card input { display: none; }
    </style>
</head>
<body>
    <div class="top-bar">Get 10% Off - Use Coupon Code RAIZO10</div>
    
    <header>
        <div class="header-top">
            <div class="header-icons" style="flex:1;">
                <i class="fas fa-search" onclick="toggleSearch()"></i>
                <i class="fas fa-bars" onclick="openSidebar()" style="margin-left:15px;"></i>
            </div>
            <div class="brand-logo" onclick="navigate('home-view')">raizo</div>
            <div class="header-icons" style="flex:1; justify-content:flex-end;">
                <i class="far fa-user" onclick="handleUserClick()"></i>
                <div class="cart-wrap" onclick="navigate('cart-view')" style="margin-left:15px;">
                    <i class="fas fa-shopping-bag"></i>
                    <span class="cart-count" id="cart-counter">0</span>
                </div>
            </div>
        </div>
    </header>

    <div id="search-container" style="display:none; padding:20px 5%; border-bottom:1px solid var(--border-color); background:#fff;">
        <input type="text" id="search-input" placeholder="Search..." onkeyup="filterProducts()" style="border:none; border-bottom:1px solid var(--text-main); background:transparent; padding:10px 0; font-size:18px;">
        <div id="search-suggestions" style="margin-top:20px;"></div>
    </div>

    <div id="sidebar-overlay" style="position:fixed; top:0; left:0; width:100%; height:100%; background:rgba(0,0,0,0.4); display:none; z-index:1500;" onclick="closeSidebar()"></div>
    <div id="sidebar" style="position:fixed; top:0; left:-300px; width:300px; height:100%; background:#fff; z-index:2000; transition:0.4s; padding:40px; display:flex; flex-direction:column;">
        <i class="fas fa-times" style="align-self:flex-end; font-size:24px; cursor:pointer; color:var(--text-muted);" onclick="closeSidebar()"></i>
        <div style="margin-top:40px; display:flex; flex-direction:column; gap:25px;">
            <a href="#" onclick="navigate('home-view'); closeSidebar();" style="text-decoration:none; color:var(--text-main); font-size:18px; letter-spacing:1px;">Shop All</a>
            <a href="#" onclick="navigate('cart-view'); closeSidebar();" style="text-decoration:none; color:var(--text-main); font-size:18px; letter-spacing:1px;">My Bag</a>
            <a href="#" onclick="handleUserClick(); closeSidebar();" style="text-decoration:none; color:var(--text-main); font-size:18px; letter-spacing:1px;">My Account</a>
            <a href="#" onclick="navigate('chat-view'); closeSidebar();" style="text-decoration:none; color:var(--text-main); font-size:18px; letter-spacing:1px;">Live Support</a>
            <a href="mailto:robiulislam786786u@gmail.com" style="text-decoration:none; color:var(--text-main); font-size:18px; letter-spacing:1px;">Contact Us</a>
        </div>
        <div style="margin-top:auto; font-size:12px; color:var(--text-muted);" id="sidebar-user-display">Not Logged In</div>
    </div>

    <div id="home-view" class="view-section active-view">
        <div style="width:100%; height:60vh; background-image: url('https://images.unsplash.com/photo-1519689680058-324335c77eba?auto=format&fit=crop&q=80'); background-size: cover; background-position: center; display:flex; align-items:center; justify-content:center;">
            <div style="background:rgba(255,255,255,0.85); padding:40px 60px; text-align:center;">
                <p style="font-size:12px; letter-spacing:2px; margin-bottom:10px;">NEW COLLECTION</p>
                <h1 style="font-size:36px; margin-bottom:20px;">Comfort & Joy</h1>
                <button class="btn-main" onclick="document.getElementById('products-grid').scrollIntoView({behavior:'smooth'})">Shop Now</button>
            </div>
        </div>
        
        <h2 class="section-title">New Arrivals</h2>
        <div class="products-grid" id="products-grid"></div>
    </div>

    <div id="product-view" class="view-section">
        <div style="max-width: 900px; margin: 40px auto; padding: 0 5%; display:flex; flex-wrap:wrap; gap:40px;">
            <div style="flex: 1; min-width: 300px; background:var(--card-bg); padding:20px;">
                <img id="pd-img" src="" style="width:100%; height:400px; object-fit:contain; mix-blend-mode:multiply;">
                <div id="pd-gallery" style="display:flex; justify-content:center; gap:10px; margin-top:20px;"></div>
            </div>
            <div style="flex: 1; min-width: 300px; display:flex; flex-direction:column; justify-content:center;">
                <h1 id="pd-title" style="font-size: 32px; margin-bottom: 10px;">Product Name</h1>
                <p id="pd-price" style="font-size: 20px; margin-bottom: 20px;">₹0</p>
                <p style="font-size: 13px; color: var(--text-muted); margin-bottom: 30px; line-height:1.8;">
                    I'm a product description. I'm a great place to add more information about your product such as sizing, material, care and cleaning instructions.
                </p>
                <ul id="pd-features" style="list-style:none; margin-bottom:30px; font-size:13px; color:var(--text-muted); border-top:1px solid var(--border-color); padding-top:20px;"></ul>
                
                <button class="btn-main" onclick="addToCartFromDetail()" style="margin-bottom:10px; width:100%;">Add to Cart</button>
                <button class="btn-outline" onclick="openCheckoutModal('single')" style="width:100%;">Buy Now</button>
            </div>
        </div>
    </div>
    <div id="cart-view" class="view-section">
        <div style="max-width: 700px; margin: 40px auto; padding: 0 5%;">
            <h2 class="section-title">Shopping Cart</h2>
            <div id="cart-items-container"></div>
            <div style="border-top:1px solid var(--border-color); padding-top:20px; text-align:right;">
                <p id="cart-total" style="font-size: 24px; font-family:'Playfair Display'; margin-bottom:20px;">Total: ₹0</p>
                <button class="btn-main" onclick="openCheckoutModal('cart')">Checkout</button>
            </div>
        </div>
    </div>

    <div id="profile-view" class="view-section">
        <div style="max-width: 600px; margin: 40px auto; padding: 0 5%;">
            <h2 class="section-title">My Account</h2>
            <div style="text-align: center; margin-bottom:40px;">
                <div style="width: 80px; height: 80px; background: var(--card-bg); border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 30px; margin: 0 auto 15px;"><i class="far fa-user"></i></div>
                <h3 id="prof-name" style="font-size: 24px;">Name</h3>
                <p style="color: var(--text-muted); font-size: 14px;"><span id="prof-phone">...</span></p>
            </div>
            
            <h3 style="font-size:20px; margin-bottom:20px; border-bottom:1px solid var(--border-color); padding-bottom:10px;">Order History</h3>
            <div id="order-history-list"></div>
            
            <button onclick="logoutUser()" class="btn-outline" style="width: 100%; margin-top: 30px;">Sign Out</button>
        </div>
    </div>

    <div id="chat-view" class="view-section">
        <div style="padding: 0 5%;">
            <h2 class="section-title">Client Concierge</h2>
            <div class="chat-container">
                <div class="chat-header-premium">
                    <h3 style="margin:0; font-size:20px;">Raizo Support</h3>
                    <p style="margin:0; font-size:12px; color:var(--text-muted);">Available to assist you</p>
                </div>
                <div class="chat-messages-area" id="user-chat-box">
                    <div style="text-align:center; margin-top:40px;">
                        <p style="color:var(--text-muted); margin-bottom:20px;">Please login to connect with our team.</p>
                        <button onclick="openLogin()" class="btn-main">Sign In</button>
                    </div>
                </div>
                <div class="chat-input-box" id="user-chat-input-wrapper" style="display:none;">
                    <i class="far fa-image" style="font-size:20px; color:var(--text-muted); cursor:pointer;" onclick="document.getElementById('user-chat-img').click()"></i>
                    <input type="file" id="user-chat-img" accept="image/*" style="display:none;" onchange="sendUserChatImage(event)">
                    <input type="text" id="user-chat-input" placeholder="Type a message..." style="margin:0; padding:10px 15px; border:none; background:var(--card-bg);">
                    <button onclick="sendUserMessage()" style="background:transparent; border:none; font-size:20px; cursor:pointer;"><i class="far fa-paper-plane"></i></button>
                </div>
            </div>
        </div>
    </div>

    <div class="chat-fab-premium" onclick="navigate('chat-view')">
        <i class="far fa-comment-dots"></i>
    </div>

    <div id="login-modal" class="modal">
        <div class="modal-content">
            <span class="close-modal" onclick="closeLogin()">&times;</span>
            <h2 style="margin-bottom: 25px; text-align: center;">Sign In</h2>
            <input type="text" id="login-name" placeholder="Full Name">
            <input type="tel" id="login-number" placeholder="Mobile Number">
            <button onclick="processLogin()" class="btn-main" style="width:100%;">Continue</button>
        </div>
    </div>

    <div id="checkout-modal" class="modal">
        <div class="modal-content" style="max-height: 90vh; overflow-y: auto;">
            <span class="close-modal" onclick="closeCheckout()">&times;</span>
            <h2 style="margin-bottom: 25px;">Checkout</h2>
            
            <form id="checkout-form">
                <p style="font-size: 12px; font-weight: 500; margin-bottom: 10px; color: var(--text-muted); text-transform:uppercase; letter-spacing:1px;">Shipping Details</p>
                <input type="text" id="chk-add1" placeholder="Address Line 1" required>
                <input type="text" id="chk-add2" placeholder="City / Area">
                <input type="text" id="chk-pin" placeholder="Postal Code" required>
                
                <div style="display: flex; gap: 10px; margin-bottom: 20px;">
                    <input type="text" id="chk-coupon" placeholder="Promo Code" style="margin:0;">
                    <button type="button" onclick="applyCouponCode()" class="btn-outline" style="padding: 0 20px;">Apply</button>
                </div>
                
                <div id="checkout-summary" style="margin-bottom: 25px; font-size: 15px; text-align: right; background: var(--card-bg); padding:20px; border:1px solid var(--border-color);">
                    Subtotal: ₹<span id="chk-subtotal">0</span><br>
                    <span id="chk-discount-row" style="color: var(--text-muted); display: none;">Discount: -₹<span id="chk-discount">0</span><br></span>
                    <div style="margin-top:10px; padding-top:10px; border-top:1px solid var(--border-color); font-size:18px;">
                        Total: <span id="chk-final-total">0</span>
                    </div>
                </div>

                <div style="margin-bottom: 25px;">
                    <p style="font-weight: 500; margin-bottom: 15px; font-size: 14px; text-transform:uppercase; letter-spacing:1px; color:var(--text-muted);">Payment Method</p>
                    <p id="upi-only-warning" style="display:none; color:#d9534f; font-size:12px; margin-bottom:10px;">Only online payment available for these items.</p>
                    
                    <label id="cod-label" class="payment-card selected" onclick="selectPayment('COD')">
                        <input type="radio" name="payment_method" value="COD" checked>
                        <span>Cash on Delivery</span>
                        <i id="cod-check" class="fas fa-circle" style="color:var(--text-main);"></i>
                    </label>

                    <label id="upi-label" class="payment-card" onclick="selectPayment('UPI')">
                        <input type="radio" name="payment_method" value="UPI">
                        <span>Pay Online (UPI)</span>
                        <i id="upi-check" class="far fa-circle" style="color:var(--text-muted);"></i>
                    </label>

                    <div id="upi-proof-section" style="display: none; margin-top: 15px; padding: 20px; border: 1px solid var(--border-color); background: var(--card-bg); text-align: center;">
                        <a id="pay-now-btn" href="#" target="_blank" class="btn-outline" style="display: block; width: 100%; margin-bottom: 15px; text-decoration:none;">Open App to Pay ₹<span id="display-upi-total">0</span></a>
                        <label for="upi-screenshot" id="upload-label" style="display: block; padding: 15px; border: 1px dashed var(--text-muted); cursor: pointer;">
                            <span id="upload-text" style="color: var(--text-muted); font-size: 13px;">Attach Payment Screenshot</span>
                        </label>
                        <input type="file" id="upi-screenshot" accept="image/*" style="display: none;" onchange="previewScreenshot(event)">
                        <img id="screenshot-preview" style="width: 100%; max-width: 200px; margin-top: 15px; display: none; border: 1px solid var(--border-color);">
                    </div>
                </div>

                <button type="submit" id="order-now-btn" class="btn-main" style="width: 100%;">Complete Purchase</button>
            </form>
        </div>
    </div>

    <footer style="text-align: center; padding: 60px 20px; border-top: 1px solid var(--border-color); margin-top: 60px;">
        <h2 style="letter-spacing:2px; margin-bottom: 20px; font-size:24px;">raizo</h2>
        <div style="display:flex; justify-content:center; gap:20px; margin-bottom:30px; font-size:20px;">
            <a href="https://youtube.com/@r.s_saru_vlog?si=G0fs6vu89ByI4r6C" target="_blank" style="color:var(--text-main);"><i class="fab fa-youtube"></i></a>
            <a href="https://www.instagram.com/rs__fashion____009?igsh=d3JtYnpoMmZ5a3Ri" target="_blank" style="color:var(--text-main);"><i class="fab fa-instagram"></i></a>
        </div>
        <p style="font-size: 12px; color: var(--text-muted);">© 2026 Raizo. Developed by Robiul Islam.</p>
    </footer>

    <script src="main.js"></script>
</body>
</html>
